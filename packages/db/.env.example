# Used by Prisma CLI (migrate / generate). Copy to `packages/db/.env`.
# PostgreSQL only (Supabase or local Postgres). Never SQLite.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/trainflow
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/trainflow
